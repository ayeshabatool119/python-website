
<!--

git clone url

cd 'directory'

touch 'creat a file'

git add 'file name'

git commit -m 'message'

git push

git pull

git log

git reset --hard 'value'

git branch

git branch 'name'

git checkout 'branchname'

git merge 'branch name'
-->